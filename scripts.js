//nameSpace
const decide = {}

//global variables
const getClass = document.getElementsByClassName("example")

//document ready

decide.document.addEventListener("DOMContentLoaded", function (event) {
  decide.init()
});

//init function

const init = () => {

}

//add inputs to the form when the user hits enter on pro
.append('<input type="text" class="nextPro" placeholder="Type your next pro here">')
//add inputs to the form when the user hits enter on con
//tally pro numbers on click
//tally con numbers on click
//evaluate which is higher and print answer
